<!--
  FreeSWITCH internal / WebRTC (WSS) profile stub for lab.

  Lab expects FREESWITCH_WSS_URL=wss://localhost:7443 (edge webrtccred default).

  Requirements for SIP.js softphone:
  - mod_sofia loaded with TLS certs (wss needs TLS)
  - wss-binding on 7443
  - auth-calls true so directory users (agent-{uuid}) can REGISTER
  - Directory domain matches SIP URI domain from edge (default: localhost)

  Production: terminate TLS at reverse proxy or use proper FS certs;
  do not expose 5060/8021 publicly (see Task 14 security checklist).

  If the base image already ships an internal.xml, merge wss-binding
  rather than duplicating the profile name.
-->
<profile name="internal">
  <aliases>
    <alias name="default"/>
  </aliases>

  <gateways/>

  <domains>
    <domain name="all" alias="true" parse="false"/>
  </domains>

  <settings>
    <param name="debug" value="0"/>
    <param name="sip-trace" value="no"/>
    <param name="rfc2833-pt" value="101"/>
    <param name="sip-port" value="5060"/>
    <param name="dialplan" value="XML"/>
    <param name="context" value="default"/>
    <param name="dtmf-duration" value="2000"/>
    <param name="inbound-codec-prefs" value="OPUS,PCMU,PCMA"/>
    <param name="outbound-codec-prefs" value="OPUS,PCMU,PCMA"/>
    <param name="hold-music" value="local_stream://moh"/>
    <param name="rtp-timer-name" value="soft"/>
    <param name="local-network-acl" value="localnet.auto"/>
    <param name="manage-presence" value="true"/>
    <param name="inbound-codec-negotiation" value="generous"/>
    <param name="nonce-ttl" value="60"/>
    <param name="auth-calls" value="true"/>
    <param name="inbound-late-negotiation" value="true"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="ext-rtp-ip" value="auto-nat"/>
    <param name="ext-sip-ip" value="auto-nat"/>
    <param name="rtp-timeout-sec" value="300"/>
    <param name="rtp-hold-timeout-sec" value="1800"/>

    <!-- WebSocket / WebRTC (SIP.js) -->
    <param name="ws-binding" value=":5066"/>
    <param name="wss-binding" value=":7443"/>
    <param name="tls" value="true"/>
    <param name="tls-only" value="false"/>
    <param name="tls-bind-params" value="transport=tls"/>
    <param name="tls-sip-port" value="5061"/>
    <param name="tls-verify-date" value="true"/>
    <param name="tls-verify-policy" value="none"/>
    <param name="tls-verify-depth" value="2"/>
    <!-- Point at lab/self-signed certs; override paths for production -->
    <param name="tls-cert-dir" value="/etc/freeswitch/tls"/>
  </settings>
</profile>
